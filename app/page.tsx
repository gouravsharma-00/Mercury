import React from 'react'
import MindReader from "@components/play"

export default async function Page() {
    // const res = await fetch("http://localhost:3000/api/gitlab")
    // const result = await res.json()

    // console.log(result)
    return (
        <MindReader />
    )
}